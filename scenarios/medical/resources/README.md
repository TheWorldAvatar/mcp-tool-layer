# Medical resources

T-Box / schema files used by medical meta-task configs live under the repo’s [`medical_case/`](../../medical_case/) tree (see `configs/meta_task/meta_task_config_medical_*.json` `ttl_file` paths).

This folder is a pointer for the scenario workspace; do not duplicate large ontology files here unless you intentionally pin a snapshot for a paper revision.
